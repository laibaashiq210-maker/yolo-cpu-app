"""
Backend package — all detection/tracking/video-processing logic.

This package has NO dependency on Streamlit (or any UI framework), so it can
be reused by a different frontend (CLI, FastAPI, another UI, tests, etc.)
without modification.
"""
